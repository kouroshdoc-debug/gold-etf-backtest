# مرحلهٔ ۴ — دروازهٔ بسته‌پیش‌فرض پایش آزمایشی

اجرای مرجع: [GitHub Actions run 23](https://github.com/kouroshdoc-debug/gold-etf-backtest/actions/runs/35208599834)

## خروجی اجرایی

- ۶ آزمون حفاظتی از ۶ آزمون موفق شد.
- عیار: موجود، ۱٬۹۲۱ ردیف، منبع آینهٔ پین‌شدهٔ TSETMC.
- گوهر: دادهٔ رسمی در دسترس Runner نبود؛ `missing`.
- کهربا: دادهٔ رسمی در دسترس Runner نبود؛ `missing`.
- پوشش اعتبارسنجی: ۱ از ۳ صندوق.
- تعداد صندوق‌های پذیرفته‌شده با دادهٔ رسمی: صفر.

| نسخه | سیگنال سایه‌ای صندوق اصلی | وضعیت ارتقا | حالت | اجرای واقعی |
|---|---|---|---|---|
| V2 | CASH | RESEARCH_ONLY | SHADOW_ONLY | غیرمجاز |
| V3 | HOLD | RESEARCH_ONLY | SHADOW_ONLY | غیرمجاز |

`HOLD` در V3 فقط بازسازی وضعیت فرضی راهبرد از ابتدای تاریخچه است؛ دستور خرید جدید یا توصیهٔ نگه‌داری سرمایهٔ واقعی نیست.

## قواعد ارتقا

ارتقا به `PAPER_WATCH_CANDIDATE` فقط وقتی ممکن است که همهٔ شروط زیر برقرار باشند:

1. دادهٔ اصلی تازه باشد.
2. دست‌کم سه صندوق مستقل تمام گیت‌های فریز‌شده را بگذرانند.
3. دست‌کم سه صندوق پذیرفته‌شده منبع رسمی TSETMC داشته باشند.
4. هر سه صندوق پیکربندی‌شده واقعا ارزیابی شده باشند.
5. حتی پس از ارتقا، `execution_authorized=false` باقی می‌ماند.

## مسیر دریافت رسمی روی لپ‌تاپ

در PowerShell و از ریشهٔ مخزن اجرا شود:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\download_official_windows.ps1
```

اسکریپت هر سه تاریخچه را مستقیما از TSETMC می‌گیرد، حداقل طول و `InsCode` را کنترل می‌کند، SHA-256 می‌سازد و فایل‌ها را در `data\official` قرار می‌دهد. پس از بازبینی، فایل‌ها باید با GitHub Desktop commit و push شوند؛ Workflow همان لحظه هر سه صندوق را بدون تغییر پارامتر ارزیابی خواهد کرد.

## قضاوت

زیرساخت پایش سایه‌ای آماده است، ولی دادهٔ رسمی هنوز ناقص است. هرگونه ارتقا در وضعیت فعلی بیش‌برازش و اعتماد کاذب ایجاد می‌کند؛ بنابراین توقف خودکار تصمیم درست سیستم است.
